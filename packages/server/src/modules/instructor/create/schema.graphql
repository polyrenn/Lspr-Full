type MutationResponse {
  errors: [Error!]
  sessionId: String
}

type Mutation {
  createInstructor(
    pictureUrl: String
    name: String!
    linkedInUrl: String
    title: String!
    description: String!
  ): MutationResponse!
}